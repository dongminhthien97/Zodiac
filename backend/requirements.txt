# Core FastAPI and web server
fastapi>=0.104.0,<0.112.0  # Compatible with Python 3.11
uvicorn[standard]>=0.24.0,<0.31.0  # ASGI server with standard dependencies

# Astrology and ephemeris libraries (keep latest stable)
kerykeion>=5.7.2,<6.0.0  # Astrological calculations
pyswisseph>=2.10.3.2  # Swiss Ephemeris for precise calculations
pytz  # Timezone support used by AstrologyService

# Database and API clients
supabase>=2.27.0,<3.0.0  # Supabase client
requests>=2.32.3  # HTTP requests (required by kerykeion)

# AI and external APIs
google-genai>=0.1.0  # Google GenAI integration (modern SDK)
groq  # Groq Python SDK (OpenAI-compatible)

# Utilities
python-dotenv>=1.0.0  # Environment variable management

# HTTP client for async requests
httpx>=0.27.0  # Async HTTP client for Groq API
